meta {
  name: Import series
  type: http
  seq: 2
}

get {
  url: {{url}}/series/import?id={{validId}}
  body: none
  auth: none
}

params:query {
  id: {{validId}}
}

settings {
  encodeUrl: true
}
