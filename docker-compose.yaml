services:
  trakt-import-service:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    image: trakt-import-service:latest
    hostname: trakt-import-service.mediamaster.internal
    container_name: trakt-import-service.mediamaster.internal
    volumes:
      - ./.env:/app/.env:ro
    networks:
      - mediamaster-internal-network

networks:
  mediamaster-internal-network:
    name: mediamaster-internal-network
